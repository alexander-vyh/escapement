# Tasks — maintainability-verifiers

Tracked in beads under epic `escapement-onor`.

## Walking skeleton

- [x] **Replay the pre-patch check across history** (`escapement-onor.1`) —
  `specs/prepatch-failure-verification.md#Severity is earned by replay, not asserted`.
  Result recorded in `design.md` → Skeleton Result. Decidability confirmed (4.3% vacuous,
  0.88s/commit); the rework hypothesis was falsified (11% vs 47.5% repair-within-24h),
  so the blocking tier does not ship.

## Verification

- [x] **Question a landing whose changed tests all pass pre-patch** (`escapement-onor.2`) —
  `specs/prepatch-failure-verification.md`. `claude/hooks/prepatch_failure_gate.py`,
  registered in `agent-surfaces/manifest.json` as Claude-ready, Codex unsupported
  (no fixture proves the Codex payload carries `cwd`).

## Measurement

- [x] **Attribute post-landing repair to the change that caused it** (`escapement-onor.3`) —
  `specs/repair-attribution.md`. `tools/repair_attribution.py`, emitting into
  `.beads/.gate-signal.jsonl`.

## Withdrawn

- [~] **Generate plugin mirrors from a single source** (`escapement-onor.4`) — withdrawn on a
  falsified premise. The mirrors are already generated by `tools/render_agent_surfaces.py`
  and drift-checked in CI. See `design.md` → Decisions.

## Carried out of scope

- **Structural design layer** (`escapement-onor.5`, P1, research) — the representation that
  is both human-reviewable and machine-checkable is unresolved. Not part of this change.
